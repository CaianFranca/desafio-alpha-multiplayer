# Infra — Flicker of Sanity

Padrões desta área: [`CODING_STANDARDS.md`](./CODING_STANDARDS.md).

## Docs atuais via Context7

Quando uma config de infra depende de sintaxe ou comportamento exato da
ferramenta — diretivas do NGINX (roteamento `/ws/`), Docker/Compose —
consulte as docs oficiais no MCP `context7` (informando a ferramenta e
versão) antes de escrever a config.
