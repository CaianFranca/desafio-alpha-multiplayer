import { StubPage } from './StubPage'
import { stubs } from '../../components/home/placeholders'

export function CriarSalaPage() {
  return <StubPage title={stubs.criarSala.title} message={stubs.criarSala.message} />
}
